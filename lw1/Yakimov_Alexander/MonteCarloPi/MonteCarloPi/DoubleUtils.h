#pragma once

using namespace std;

class DoubleUtils
{
public:
	static bool areEqual(double lValue, double rValue);
	static bool isMore(double lValue, double rValue);
	static bool isLessOrEqual(double lValue, double rValue);
};
